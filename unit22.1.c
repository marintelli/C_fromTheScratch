#include <stdio.h>
#include <stdbool.h>    // bool, true, false가 정의된 헤더 파일

// stdboo,h 이용

int main()
{
    bool b1 = true;
    if(b1 == true)
    printf("참\n");
    else
    printf("거짓\n");  
    
    return 0; 
    
}